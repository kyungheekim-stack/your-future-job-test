node_modules/
.next/
out/
.env
.env.local
.DS_Store
*.tsbuildinfo
next-env.d.ts

# 시안 대조용 스크래치 (커밋하지 않는다)
_compare/

# 토큰 등록용 임시 파일 — 절대 커밋 금지
token.txt

# 시안 원본 이미지 (용량이 커서 레포에 넣지 않는다)
시안_*.png
*_new.png
*_new.gif
BG-*.png
